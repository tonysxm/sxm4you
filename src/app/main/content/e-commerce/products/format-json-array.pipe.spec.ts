import { FormatJsonArrayPipe } from './format-json-array.pipe';

describe('FormatJsonArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatJsonArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
